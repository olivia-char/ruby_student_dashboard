<div class="container">
	<h1><%= @dojo.branch %> <%= @student.first_name %><%= @student.last_name %></h1>

	<table class="table">
	    <thead>
	      <tr>
	        <th>First Name:</th>
	        <th>Last Name:</th>
	        <th>Email:</th>
	      </tr>
	    </thead>
	    <tbody>
	      <tr>
	        <td><%= @student.first_name %></td>
	        <td><%= @student.last_name %></td>
	        <td><%= @student.email %></td>
	      </tr>
	    </tbody>
	</table>

	<a href="/dojos">Home</a> | <a href="/dojos/<%= @dojo.id %>/students/<%= @student.id %>/edit">Edit</a>
</div>
